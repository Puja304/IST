#ifndef ALL_CONSTS_H
#define ALL_CONSTS_H


const int MAX_DESCRIPTION_SIZE = 30;            // max length for description
const int MAX_PRODUCT_NAME_SIZE = 10;           // max length for product name
const int MAX_RELEASE_ID_SIZE = 8;              // max length for release ID
const int DATE_SIZE = 11;                       // length of date of request
const int PHONE_SIZE = 11;                      // length of requester phone number
const int MAX_REQUESTER_NAME_SIZE = 30;         // max length of requester name
const int SIZE_OF_ACCESSED_INDEX_CACHE = 20;    // length of previously accessed index cache
const int ID_DIGITS = 8;
const int STATUS_DIGITS = 2;
const int RELEASE_DATE_SIZE = 11;               // length of release date
const int PHONE_NUMBER_SIZE = 11;               // length of phone number
const int MAX_EMAIL_SIZE = 24;                  // max length for email address
const int MAX_DEPARTMENT_SIZE = 12;             // max length for department name
const int MAX_PRINTS = 16;

#endif
